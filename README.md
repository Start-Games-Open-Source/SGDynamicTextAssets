# SGDynamicTextAssets
The Start Games Dynamic Text Assets is an Unreal Engine plugin that provides a text-based (JSON, XML, YAML, can extend to others) data configuration system similar to Primary Data Assets but stored as external text files. These files serve as the source of truth for static game data configuration.
